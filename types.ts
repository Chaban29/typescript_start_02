const isFetching: boolean = true;
const isLoading: boolean = false;

const int: number = 23;
const float: number = 2.3;
const num: number = 3e10;

const myDogName: string = 'Ricky';
let myDogHasLegs: any = 4;

myDogHasLegs = 'Has 4 legs';

const someMessage: string = 'Hello TypeScript';

const numberArray: number[] = [1, 2, 3, 4, 5, 6];

const tuplesArray: Array<number> = [10, 20, 30, 40, 50];

const someArr: [string, number, boolean] = ['Hello', 23, true];

let variable: any = 100;

variable = 'New String';

console.log(variable);

const showMyName = (userName: string): void => {
  console.log(userName);
};

showMyName('Roman');


